# Monastery dogfood todos

## Current PR

- #74 is merged; #73 is closed.
- Structured artifact P0/P1 core fail-fast repair behavior is now on main.
- #80 is draft and should not be merged yet; it needs lock ownership (`ownerToken`/`hostname`), owner-token release, signal cleanup, and safer force/stale handling.

## Suggested order

1. #77 repo-level step lock guardrail.
2. #75 phase-level progress, timing, and runtime budgets.
3. #78 associated PR comments/reviews/checks in maintainer context.
4. #79 rework existing draft PR from human feedback.
5. #76 outward language policy and enforcement.
6. Return to #73/#74 once feedback-loop support is clearer.

## Notes

- Use `--dry-run` before real `step`.
- Avoid merging draft PRs generated by monastery until human review passes.
